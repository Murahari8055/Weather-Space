# TODO: Add Welcome Endpoint to Spring Boot API

- [x] Add necessary imports to WeatherController.java (Logger, LoggerFactory, HttpServletRequest, Map, HashMap)
- [x] Add logger field to WeatherController.java
- [x] Add new @GetMapping("/welcome") endpoint that logs request method and path, and returns JSON welcome message
- [ ] Rebuild the Spring Boot application (in progress)
- [ ] Redeploy the application to the remote server
- [ ] Test the new endpoint by making a GET request to /api/weather/welcome
